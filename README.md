# Dockerized APEX Instance
### by Viscosity North America

This is repo contains the files to build a docker image to launch containers running `Oracle Database` with `Oracle Application Express` installed.
